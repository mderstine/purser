# README Chart Fixes

## Job To Be Done
Fix rendering issues in the three diagrams in README.md so they display correctly.

## Requirements
- The mermaid Workflow Diagram must use proper newlines instead of literal `\n` characters
- The Architecture (L0 Core) ASCII art box drawing must have properly aligned characters
- The Steering Model ASCII art box drawing must have properly aligned characters
- All three diagrams must render correctly in GitHub's markdown preview

## Constraints
- Keep the same diagram content and structure; only fix rendering issues
- Mermaid diagram must remain valid mermaid syntax
- ASCII art diagrams must use consistent box-drawing characters and alignment

## Notes
- The Workflow Diagram is a mermaid flowchart (lines 36-82 of README.md)
- The Architecture (L0 Core) diagram is ASCII art (lines 365-386)
- The Steering Model diagram is ASCII art (lines 397-422)
